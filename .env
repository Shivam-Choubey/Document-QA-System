# This project runs 100% locally, so no API keys are required today.
#
# This file exists so that if you later extend the project (for example,
# swapping in a paid API model, or adding configuration secrets), you
# already have a standard place to put them without changing your code.
#
# Example of what might go here in the future:
# OPENAI_API_KEY=your-key-here
